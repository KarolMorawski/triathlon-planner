[pytest]
# Default collection = the permanent pure-logic suite only (no network).
# test_session.py is an INTERACTIVE Garmin integration test — run it directly
# with `python3 test_session.py`; a bare `pytest` would block on its input().
testpaths = tests
